apps:
  - script: src/index.js
    name: network-api
    max_restarts: 5
    error_file: logs/network-api.stderr.log
    out_file: logs/network-api.stdout.log
    pid_file: pids/network-api.pid
    env:
      NODE_ENV: production
